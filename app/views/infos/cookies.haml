= render :partial => "layouts/left_hand_navigation", :locals => { :links => about_links }

- content_for :title, t('layouts.infos.cookies').html_safe

- content_for :title_header do
  %h1= t("layouts.infos.info_about_kassi")

.left-navi-section.about-section
  = render :layout => "layouts/mercury_editable_page", :locals => { :content_type => "cookies_page_content" } do
    - content = cookies_page_content.html_safe

    - content_for :meta_description, StringUtils.first_words(strip_tags(content))

    - content
